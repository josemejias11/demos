{
  "name": "playwright-test-suite",
  "version": "1.0.0",
  "description": "Minimal Playwright test suite setup",
  "scripts": {
    "test": "playwright test",
    "playwright": "playwright test"
  },
  "devDependencies": {
    "playwright": "^1.55.0",
    "@playwright/test": "^1.55.0"
  }
}
